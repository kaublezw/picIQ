# picIQ Frontend

React application that displays MLB picks and their outcomes fetched from the
backend API.

## Setup

1. `npm install`
2. `npm start` to run the development server on port 3000.

During development, configure a proxy in `package.json` if backend runs on a
different port.
